runtime: python
env: flex
entrypoint: gunicorn -b :$PORT main:app

runtime_config:
  python_version: 3

# Incurs costs to run on the App Engine flexible environment
# Settings below are to reduce costs during testing and are not for production use
manual_scaling:
  instances: 1
resources:
  cpu: 1
  memory_gb: 0.5
